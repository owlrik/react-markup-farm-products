import styled from "styled-components";
import { Ul } from "../../styled";

const StyledFeaturesList = styled(Ul)`
  display: flex;
  flex-direction: column;
  gap: 20px;
  margin: 0;
  padding: 0;
  list-style: none;
`;

export { StyledFeaturesList };
